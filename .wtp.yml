version: "1.0"
defaults:
  base_dir: ../worktrees/politely

hooks:
  post_create:
    - type: copy
      from: .claude
      to: .claude

    - type: command
      command: mise trust

    - type: command
      command: bun install
